public class Main {
    public static void main(String[] args) {
        int a = 91;
        int b = 5;
        int c = 9;
        int d = 3;
        int e = 8;
        int f = -35;
        int g = 2;
        int h = 5;
        int i = 4;
        int j = 12;

        int sum = a + b + c + d + e + f + g + h + i + j;
        System.out.println("Сума: " + sum);

        int difference = a - b - c - d - e - f - g - h - i - j;
        System.out.println("Різниця: " + difference);

        int product = a * b * c * d * e * f * g * h * i * j;
        System.out.println("Добуток: " + product);

        int quotient = a / b / c / d / e / f / g / h / i / j;
        System.out.println("Частка: " + quotient);
    }
}
